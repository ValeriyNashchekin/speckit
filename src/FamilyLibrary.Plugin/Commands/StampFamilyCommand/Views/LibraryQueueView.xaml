<Window
    x:Class="FamilyLibrary.Plugin.Commands.StampFamilyCommand.Views.LibraryQueueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:FamilyLibrary.Plugin.Commands.StampFamilyCommand.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModel:LibraryQueueViewModel}"
    Title="Family Library Queue"
    Width="700"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status Bar -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding StatusSummary}" VerticalAlignment="Center" FontWeight="Bold"/>
            </Grid>
        </Border>

        <!-- Tab Control -->
        <TabControl Grid.Row="1">
            <!-- Tab 1: All Families -->
            <TabItem Header="All Families">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Loadable Families in Document" Style="{StaticResource HeaderTextStyle}"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Families}"
                              Style="{StaticResource DataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Binding="{Binding IsSelected}" Header="Select" Width="50"/>
                            <DataGridTextColumn Binding="{Binding FamilyName}" Header="Family Name" Width="*"/>
                            <DataGridTextColumn Binding="{Binding CategoryName}" Header="Category" Width="150"/>
                            <DataGridCheckBoxColumn Binding="{Binding HasStamp}" Header="Stamped" Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Scan" Command="{Binding ScanCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                        <Button Content="Add to Queue" Command="{Binding AddToQueueCommand}" Style="{StaticResource ButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Tab 2: Queue -->
            <TabItem Header="Queue">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Processing Queue" Style="{StaticResource HeaderTextStyle}"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding QueueItems}"
                              Style="{StaticResource DataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding FamilyName}" Header="Family Name" Width="*"/>
                            <DataGridTextColumn Binding="{Binding CategoryName}" Header="Category" Width="150"/>
                            <DataGridTextColumn Binding="{Binding Status}" Header="Status" Width="80"/>
                            <DataGridTextColumn Binding="{Binding ErrorMessage}" Header="Error" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Stamp" Command="{Binding StampCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                        <Button Content="Publish" Command="{Binding PublishCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                        <Button Content="Clear Queue" Command="{Binding ClearQueueCommand}" Style="{StaticResource ButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Tab 3: Status -->
            <TabItem Header="Status">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Processing Summary" Style="{StaticResource HeaderTextStyle}"/>

                    <Grid Grid.Row="1" Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Scanned:" Margin="5"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ScannedCount}" Margin="5" FontWeight="Bold"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Stamped:" Margin="20,5,5,5"/>
                        <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding StampedCount}" Margin="5" FontWeight="Bold"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Published:" Margin="5"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PublishedCount}" Margin="5" FontWeight="Bold"/>

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Failed:" Margin="20,5,5,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding FailedCount}" Margin="5" FontWeight="Bold" Foreground="Red"/>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Close" IsCancel="True" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>