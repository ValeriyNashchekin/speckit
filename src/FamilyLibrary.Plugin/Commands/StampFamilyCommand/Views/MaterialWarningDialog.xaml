<Window
    x:Class="FamilyLibrary.Plugin.Commands.StampFamilyCommand.Views.MaterialWarningDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:FamilyLibrary.Plugin.Commands.StampFamilyCommand.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModel:MaterialWarningViewModel}"
    Title="Material Warning"
    Width="500"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="WarningIconStyle" TargetType="Path">
            <Setter Property="Fill" Value="#F59E0B"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="MessageTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
        </Style>
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="IsReadOnly" Value="False"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Warning icon and message -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Warning triangle icon -->
            <Path Grid.Column="0" Style="{StaticResource WarningIconStyle}">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="12,2">
                            <LineSegment Point="22,20"/>
                            <LineSegment Point="2,20"/>
                            <LineSegment Point="12,2"/>
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>

            <TextBlock Grid.Column="1"
                       Text="Some materials may not exist in this project"
                       Style="{StaticResource MessageTextStyle}"/>
        </Grid>

        <!-- Materials list -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding MissingMaterials}"
                  Style="{StaticResource DataGridStyle}"
                  MaxHeight="250">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Material Name"
                                    Binding="{Binding Name}"
                                    Width="*"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Used In Layer"
                                    Binding="{Binding LayerFunction}"
                                    Width="*"
                                    IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Create New"
                                        Binding="{Binding CreateNew, UpdateSourceTrigger=PropertyChanged}"
                                        Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    IsCancel="True"/>
            <Button Grid.Column="3"
                    Content="Proceed Anyway"
                    Command="{Binding ProceedCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsDefault="True"/>
        </Grid>
    </Grid>
</Window>
